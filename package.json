{
  "name": "whose-turn",
  "version": "0.1.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "deploy": "zip -qr lambda.zip .; aws lambda update-function-code --function whoseTurnIsIt --zip-file fileb://lambda.zip >/dev/null && echo 'Uploaded!'; rm lambda.zip",
    "start": "node index.js",
    "watch": "npx babel --watch src --out-dir . --presets babel-preset-react-app/prod >/dev/null 2>&1 &"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@babel/cli": "^7.21.0",
    "babel-preset-react-app": "^10.0.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
  }
}
